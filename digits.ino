void rakamSifir(int offset, uint32_t renk){
    stripSaat.fill(renk, (0 + offset), 3);
    stripSaat.fill(renk, (12 + offset), 3);
}

void rakamBir(int offset, uint32_t renk){
    stripSaat.fill(renk, (0 + offset), 3);
    stripSaat.fill(renk, (12 + offset), 3);
}

void rakamIki(int offset, uint32_t renk){
    stripSaat.fill(renk, (0 + offset), 6);
    stripSaat.fill(renk, (9 + offset), 3);
    stripSaat.fill(renk, (15 + offset), 6);
}

void rakamUc(int offset, uint32_t renk){
    stripSaat.fill(renk, (0 + offset), 6);
    stripSaat.fill(renk, (9 + offset), 9);
}

void rakamDort(int offset, uint32_t renk){
    stripSaat.fill(renk, (0 + offset), 3);
    stripSaat.fill(renk, (6 + offset), 9);
}

void rakamBes(int offset, uint32_t renk){
    stripSaat.fill(renk, (3 + offset), 15);
}

void rakamAlti(int offset, uint32_t renk){
    stripSaat.fill(renk, (3 + offset), 18);
}

void rakamYedi(int offset, uint32_t renk){
    stripSaat.fill(renk, (0 + offset), 6);
    stripSaat.fill(renk, (12 + offset), 3);
}

void rakamSekiz(int offset, uint32_t renk){
    stripSaat.fill(renk, (0 + offset), 21);
}

void rakamDokuz(int offset, uint32_t renk){
    stripSaat.fill(renk, (0 + offset), 15);
}
